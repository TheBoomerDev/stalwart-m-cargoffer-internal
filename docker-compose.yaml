services:
  stalwart:
    image: stalwartlabs/stalwart:latest
    restart: unless-stopped
    network_mode: host
    volumes:
      - stalwart-data:/opt/stalwart/data
      - stalwart-etc:/opt/stalwart/etc
      - ./etc/config.toml:/opt/stalwart/etc/config.toml:ro
volumes:
  stalwart-data:
  stalwart-etc:
